# Backend Environment Variables Template
# Copy this to Railway environment variables

# Spring Profile
SPRING_PROFILES_ACTIVE=prod

# Database (Railway provides these automatically via ${{Postgres.*}})
DATABASE_URL=${{Postgres.DATABASE_URL}}
PGHOST=${{Postgres.PGHOST}}
PGPORT=${{Postgres.PGPORT}}
PGDATABASE=${{Postgres.PGDATABASE}}
PGUSER=${{Postgres.PGUSER}}
PGPASSWORD=${{Postgres.PGPASSWORD}}

# JWT Configuration (CHANGE THIS!)
JWT_SECRET=your-super-secret-jwt-key-at-least-256-bits-long-change-in-production

# CORS (Update with your frontend URL)
CORS_ALLOWED_ORIGINS=https://your-frontend.railway.app,http://localhost:3000

# Server Port (Railway assigns automatically)
PORT=8080

# Email Configuration (Optional - use SendGrid, Mailgun, etc.)
# MAIL_HOST=smtp.sendgrid.net
# MAIL_PORT=587
# MAIL_USERNAME=apikey
# MAIL_PASSWORD=your-sendgrid-api-key
# MAIL_FROM=noreply@yourdomain.com
